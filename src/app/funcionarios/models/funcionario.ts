
export interface Funcionario {
    idFuncionario?: number  
    nome:string
    email:string
    foto:string
}






 // ? campo não obrigatório id vai gerado automatico, ! é usado em classe neste caso é interface
 // ! utilizado em classe para indicar que a váriavel pode ficar vazia ex (propriedade) nome!:string para informar que no momento inicial ela pode ficar vazio